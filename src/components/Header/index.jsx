import React from "react";
import { Container, Logo } from "./styles";

const Header = () => {
  return (
    <Container>
      <Logo>Dashboard</Logo>
    </Container>
  );
};

export default Header;
